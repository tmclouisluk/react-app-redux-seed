import { UIState } from '../constants/models/UI';
import * as types from '../constants/actionTypes'

const UI = (state = UIState, action) => {
    //console.log(action);
    switch (action.type) {
        default:
            return state
    }
}

export default UI;